<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="theme-color" content="#0f0f0f">
    <meta name="author" content="Bjoern Pfoster">
    <meta name="description" content="Die offizielle WAVE Trophy App. Schaue vorbei für mehr Informationen.">
    <meta name="keywords" content="WAVE, Trophy, App,">
    <meta name="robots" content="follow, index">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <base href="{{ base_url() }}/">
    <base name="xhr-baseurl" href="{{ path_for('root', { 'language': language }) }}">
    <link rel="canonical" href="{{ path_for('root') }}"/>
    <link rel="icon" href="img/wave.ico">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style.css">

    <script type="application/javascript" src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
    <script type="application/javascript" src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script type="application/javascript" src="js/mustache.min.js"></script>
    <script type="application/javascript" src="js/script.js"></script>

    <title>{{ page }} | WAVE Trophy App</title>
    {% block assets %}  {% endblock %}
</head>
<body>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('root', { 'language': language }) }}"><img src="img/wave.png" class="brand" alt="WAVE Trophy App"></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                {% if is_logged_in == false %}
                <li class="{{ (active == 'login') ? 'active' : '' }}">
                    <a href="{{ path_for('get.login', { 'language': language }) }}">{{ __('Login') }}</a>
                </li>
                {% endif %}

                {% if is_logged_in %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path_for('get.logout', { 'language': language }) }}">{{ __('Log out') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    {% block content %}{% endblock %}
</div>

<div id="loader">
    <div id="loading"></div>
</div>
<input type="hidden" data-id="language" value="{{ language }}">
</body>
</html>